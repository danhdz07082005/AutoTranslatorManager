window.ATM = window.ATM || {};

window.ATM.Editor = (function() {
    return {
        init: () => {
            // Placeholder: The actual dictionary / text editor logic goes here.
            // Currently delegating from games.js
        },
        open: (gameId) => {
            console.log("Opening editor for", gameId);
        }
    };
})();
